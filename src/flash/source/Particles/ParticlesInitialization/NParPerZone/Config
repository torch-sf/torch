# Define a preprocessor symbol so code can check whether this implementation is compiled in.
PPDEFINE PART_INITMETHOD_NPARPERZONE
D Creates tracer particle cube with pt_parPerSide particles per side per zone in a block.

PARAMETER pt_parPerSide INTEGER 2
